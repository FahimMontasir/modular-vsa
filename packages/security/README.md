# Security Package

Placeholder for security‑related services (e.g., rate‑limiting, IP blocking, JWT revocation).

## Current Status

The package is empty and serves as a future extension point. Add services under `src/` when security features are needed.
